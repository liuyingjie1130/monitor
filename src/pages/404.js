export default function ({dispatch}){
    return (
      <div style={{display: 'flex', justifyContent:'center'}}>
        <div style={{display: 'flex', flexDirection:'column', marginTop:60, alignItems:'center'}}>
        <span style={{marginTop: 30, color: '#333', fontSize:16}}>抱歉，您访问的页面不存在</span>
        </div>
      </div>
    )
  }